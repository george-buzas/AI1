variables:
  queen[12] : integer;

domains:
  queen <- [0..11];

constraints:
  alldiff(queen);
  forall (q in [0..11])
    forall (row in [1..(11-q)])
      row <> abs(queen[q] - queen[q + row]);
    end
  end

solutions: all
